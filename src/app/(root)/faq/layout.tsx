export const metadata = {
  title: "FAQ",
  description: "Frequently Asked Questions",
};

const FAQLayout: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <div>{children}</div>;
};

export default FAQLayout;
