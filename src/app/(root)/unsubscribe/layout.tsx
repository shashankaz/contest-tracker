export const metadata = {
  title: "Unsubscribe",
  description: "Unsubscribe from our mailing list",
};

const UnsubscribeLayout: React.FC<{ children: React.ReactNode }> = ({
  children,
}) => {
  return <div>{children}</div>;
};

export default UnsubscribeLayout;
